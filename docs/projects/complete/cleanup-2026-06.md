# Cleanup archive — June 2026

> Archive of completed items from [../cleanup.md](../cleanup.md). Each entry keeps its
> original text + implementation notes verbatim, since the notes document non-obvious
> decisions (root causes, rejected alternatives, caveats). Open items live in
> `../cleanup.md`; larger spun-out work lives in `../future/`.

## Done

[DONE 2026-06-06] The slot selector in the left nav bar should not be duplicated like it currently ys (the avatar block + slot-selector combobox both showed the character name; merged into one `SlotSwitcher` — avatar + name + archetype/level that IS the slot dropdown, per the shadcn team-switcher)

[DONE 2026-06-06] On the interative map route, we shoudl use different ui elements for the switcher bweteen the maps and the preset options like Discovered and undiscovered graces. (under-map controls regrouped into labeled clusters: Map = segmented control, Layers = Switch toggles, Quick select = labeled preset buttons. Also: removed Calibrate button + the info tooltip, added a bottom-right zoom/center readout, finer 0.25 zoom increments, and x,y coords in every pin popup.)

[DONE 2026-06-06] We should probably implement a simple layer like system on the map route below the map component that would allow us to hide (but not clear) 3 different layers of content: graces, bosses, items. (now shadcn Switch toggles)

[DONE 2026-06-06] The discovered graces should have a different shade of yellow than undiscovered graces.
Same for bosses but with red. (graces: found=bright gold / undiscovered=muted; bosses: defeated=bright red / remaining=muted; legend updated)

[DONE 2026-06-06] We should limit how for the leaflet map can be zoomed out, it goes too far out. (minZoom clamped to the whole-map-fits zoom, recomputed on resize so it actually sticks even if the container is unsized on first render)

[DONE 2026-06-06] There should be a button to center the map on the players location at a specific zoom level. ("Center on me" button; switches realm if needed. Disabled only when (a) no save is connected or (b) the character is in an interior/dungeon we can't project onto the overworld tile map — title text now says which; the player IS always on some map, but we only render overworld masters M00/M10, not legacy-dungeon interiors.)

[DONE 2026-06-06] We should also render the players dropped graces if they have died without picking them up, the hover on that pin should show how many are on the ground. (this is the souls memory field I believe) (the save already parses `blood_stain` = {coords, map_id, runes}; added a `useBloodstainPin` + a distinct glowing-gold-diamond marker. Hover tooltip shows "N runes on the ground"; click popup shows the count + x,y. Two states: `runes > 0` = active "Lost runes" (bright filled diamond); `runes <= 0` = a faded hollow "Last death · runes recovered" diamond. Reuses `playerToMasterPixel`, so it projects through legacy dungeons too.)

[DONE 2026-06-06] The footer is pretty tall, I think we should try to shorten it. Not sure if we need the view on github button in both the side bar and on the footer. (shortened; removed the footer's View-on-GitHub button since the sidebar has one)

[DONE 2026-06-06 ×2] We should look at the premade shadcn sidebar blocks, they have two things we miht want to take. (both things were taken — the org/team-switcher and the collapsible sidebar; see the two entries below)

[DONE 2026-06-06] On the shadcn sidebar, I know there is a premade like organiztion switcher, we could probably reuse most of that for the slot selector. (built `SlotSwitcher` from the cloned sidebar-07 team-switcher pattern at docs/cloned-repos-as-docs/ui)

[DONE 2026-06-06] We should probably make the side bar collapsable like the sidebar in the shadcn docs. (adopted the real shadcn Sidebar primitive — was 100% hand-rolled `<aside>` before, using none of the shadcn parts. Added `ui/sidebar.tsx` (Base UI `useRender` + inline Tailwind, copied verbatim from the resolved `base-nova` style at docs/cloned-repos-as-docs/ui/.../styles/base-nova — our configured `components.json` style — instead of running the CLI, which would clobber the customized tooltip.tsx), plus `ui/sheet.tsx` + `hooks/use-mobile.ts` deps and `--sidebar-*` theme tokens harmonized to our warm-stone palette. Rebuilt `AppSidebar` + `SlotSwitcher` on the primitives (`collapsible="icon"` rail; SlotSwitcher = the team-switcher pattern). Wrapped `_app.tsx` in `SidebarProvider`/`SidebarInset`; top bar now has a `SidebarTrigger` (collapses rail on desktop, opens off-canvas Sheet on mobile) and the duplicated mobile tab-nav row was removed in favor of that Sheet. Verified: client+SSR build + SSR render both clean.)

[DONE 2026-06-06] On the events data table, we have event types for maps, wetblades, cookbooks. I know we probably made this different because that show up different in the save file, but to the user those are just items. So we should probably put them with the other itemm tables like armaments and armor... etc (maps/whetblades/cookbooks were already `category: "Key Item"` in GOODS, so they already lived in the inventory tables — the events table was duplicating them. Removed `map`/`cookbook`/`whetblade` from `vm/events.ts` (now grace+boss only, card renamed "World Progress"). While here, also reorganized the inventory tables to mirror Elden Ring's own inventory tabs (the 19 dataset-native categories → 13 in-game tabs): merged Sorceries+Incantations → "Spells"; folded Consumables/Physick/Crystal Tears/Crafting Tools/Cookbooks → "Tools"; folded Remembrances/Great Runes into "Key Items"; renamed Upgrade Materials → "Bolstering Materials" and Armaments → "Weapons & Shields". Spirit Ashes keeps its own tab (game files it under Tools as "Summons", but its enriched HP/FP/summon columns warrant a dedicated table). Added a Category column to the merged Spells/Tools/Key Items tables so sub-types stay distinguishable. CAVEAT / needs a manual eyeball when the app is next run: the **Tools** tab is now large — it unions Consumables + Crystal Tears + Wondrous Physick + Crafting Tools + Cookbooks into one list. The new Category column + its facet filter should keep it navigable, but if it feels unwieldy in practice that argues for the granular-grouping alternative instead (keep the 19 dataset-native tables but just regroup/relabel the category-picker groups to mirror the in-game tabs — no table merging, nothing folded away).)

[DONE 2026-06-07] Not sure if I would like this but I think it might be cook to split up the different tables in the inventory route into different routes. We can looks to see if shadcn has a sample side bar with nested routes. (used the shadcn `nav-main` Collapsible + `SidebarMenuSub` pattern. **Inventory** is now a collapsible sidebar group with one sub-route per in-game tab — the 13 `InventoryTableType`s (Weapons & Shields, Ammunition, Armor, Talismans, Ashes of War, Spells, Spirit Ashes, Tools, Crafting Materials, Bolstering Materials, Key Items, Info Items, Gestures) — served by a single dynamic route `_app/inventory/$category.tsx` (slug↔key↔label single-sourced in `lib/inventory-tables.ts`; the card's CategoryPicker now navigates instead of writing a kvs atom; bare `/inventory` + unknown slugs redirect to weapons-shields). **Events** and **World Progress** (the old Regions table) promoted to top-level nav items (`/events`, `/world-progress`). The standalone Weapons browser (`weapons-data-table.tsx`, no icons) was dropped from nav/routes in favor of the icon-bearing Weapons & Shields table — the component file is now orphaned and can be deleted. Top bar title falls back to the longest parent prefix so category pages show "Inventory".)

[DONE 2026-06-07] Follow-up: the "Events" + "World Progress" nav items from the split above were poorly organized — "Events" actually rendered graces **and** bosses (its card was even mis-titled "World Progress"), duplicating the richer dedicated `/bosses` route, while `/world-progress` rendered the Regions table (titled "Regions"). Restructured: **Events → `/graces` "Sites of Grace"** (FlameIcon), graces-only (filters `eventsDbView` to `type === 'grace'`; `eventsDbView` itself is unchanged because it still feeds map pins + Overview/footer stats; the grace table keeps `tableId='events'` so column state + map pin-selection sync are shared). **Bosses stays top-level** (marquee feature w/ StoryBossSection). **Regions dropped from nav** — folded into the Overview as a card (`RegionsDataTable`), since discovered/undiscovered regions are a completion stat with no map markers to browse to (rationale: a collapsible "World Progress" group only earns its click-cost with several similar browse targets, and with bosses+graces top-level it'd hold just Regions + the high-traffic graces table — not worth it). Feeds toward [overview-completion-tracker.md](../overview-completion-tracker.md). Build + typecheck green.

[DONE 2026-06-07] Sidebar nav links now show a loading state while the destination route's loader runs: TanStack `<Link>` stamps `data-transitioning` on its anchor (post-hydration), and since our links render _through_ `<Link>` via Base UI's `render` prop that attribute lands on the menu-button element — a `group-data-transitioning/navlink` Tailwind variant swaps the leading icon → `Spinner` on top-level items, and shows a trailing spinner on the icon-less inventory sub-routes.

[DONE 2026-06-07] We need to make a better notfound component in the app for unknown routes. (replaced the bare `defaultNotFoundComponent: () => <div>Not Found</div>` with a themed `NotFound` in `components/shell/not-found.tsx` — a Compass/"lost off the edge of the map" 404 showing the attempted path + "Return to the map" / "Go back" actions — wired simply as the router's `defaultNotFoundComponent`. It renders full-page (no sidebar) because `_app` is a _pathless_ layout that unmatched paths don't descend into; that's fine and intentional — an earlier attempt to force it inside the shell via a splat route + `setResponseStatus`/`AppShell` wrapper was over-engineered and reverted. `/nonsense` → real 404 + themed page; real routes → 200. Also added a route-wide `defaultPendingComponent` (`route-pending.tsx`, centered spinner) shown while a route resolves — mostly during lazy chunk loads, since our data comes from in-memory atoms not async loaders — and enabled `defaultPreload: 'intent'` so a route's chunk prefetches on link hover/focus, making navigation feel instant (pending UI + Link spinner are the fallback). Decided against per-route `pendingComponent` skeletons: with no async loaders the pending state rarely fires, so tailored skeletons would be near-invisible — low ROI.)

[DONE 2026-06-07] I hate that all the data tables are paginated with only like 10 items rendering at once, we should probably implement tanstack virtual to do virtualized scrolling. I know that tanstack table or vitual has usage examples of combining tanstack table and virtual, (we should clone them to docs/cloned-repos-as-docs/ and look at the examples there). The data tables should just take up max height they can probably. This max height change will be easier on pages that only have a datatable. Not sure what to do about pages with other content? Maybe those data tables should be moved to their own routes? Not sure. (Pagination is gone — the shared `DataTable` now renders ALL filtered rows via `@tanstack/react-virtual` (`useVirtualizer`, fixed 44px rows, overscan 12). Cloned both upstream examples to `docs/cloned-repos-as-docs/tanstack-table` (v9 `virtualized-rows` — illustrative only, uses the new `useTable`/`features` API) and `docs/cloned-repos-as-docs/tanstack-virtual` (the `table` example, which is v8 `useReactTable` + react-virtual ^3.14 — an EXACT match for our stack and the pattern we followed). **Layout:** the table body is its own `overflow-auto` scroll viewport with a `sticky` header; rows are CSS-grid/flex (not auto table-layout) so column widths come straight from `getSize()` and stay stable as rows virtualize in/out (the `Name` column `flex-grow`s to absorb slack; everything else is fixed-basis + no-shrink so wide tables keep their width and scroll horizontally). **Height — chose flex-fill + app-shell rework (the user's pick) over a plain max-height cap or splitting tables onto their own routes:** `_app.tsx` is now a fixed-height frame (`SidebarProvider h-svh overflow-hidden` → one inner `overflow-y-auto` region) so a `fill` DataTable can take `flex-1 min-h-0` and fill the viewport exactly on table-only routes (`/inventory/$category`, `/graces` — their Card/CardContent are now `flex min-h-0 flex-1 flex-col`). **Mixed-content pages need NO route moves** — they just leave `fill` off and the body caps at `maxBodyHeight` (default `70vh`) and scrolls internally (bosses gallery+table, calculator advisors+AR table, overview Regions card). Deleted the now-unused `data-table-pagination.tsx`; the old pagination bar is replaced by a small "{n} pinned · {N} rows" summary. SSR-safe (the virtualizer's scroll ref is null during SSR + the hydration render → 0 rows both times, no mismatch; rows fill post-mount) — verified HTTP 200 + table shell/headers in the SSR HTML. Typecheck + oxlint + client/SSR build + the real-Chromium perf test (mount+filter over the full armaments catalog) all green. CAVEAT: with fixed `getSize()` widths, content longer than its column now clips (`overflow-hidden`) instead of auto-expanding the column — columns are resizable + persisted, but a few content-heavy columns (e.g. Effects) may want larger default `size`s as a follow-up.)

[DONE 2026-06-06] All the stuff that once was in the app bar has moved to the sidebar except the darkmode toggle. Maybe we should just move it to the side bar too. (moved to the sidebar brand header; still surfaced in the mobile top bar where the sidebar is hidden)

[DONE 2026-06-08] On the bosses screen, I do not know if we are being accurate on which bosses we are calling demigods and which we are not. We should check the wikis to be sure. Also need to add dlc bosses there. And if a boss is not defeted, (or if it is) I think there should be some way to click on the boss card to have its location on the map. Also some of the big map icons still do not have images. I think this is because we are using the remembreance icons and some of them do not drop remembrance items (so they do not have an icon to use). (all addressed in `lib/boss-meta.ts` + `sections/story-boss-section.tsx`: **classification** is now wiki-verified — `DEMIGOD_FLAGS` (Marika/Radagon bloodline) and `SHARDBEARER_FLAGS` (Great Rune holders, incl. Rennala's Unborn rune which is correctly excluded from the Leyndell gate) are distinct curated sets with a header citing the Fextralife Demigods/Great Runes/Remembrance pages; `bossBadges()` surfaces Demigod/Shardbearer/Legend then falls back to a Field/Dungeon/Great-Enemy tier from the arena map id. **DLC bosses** added across two gallery groups (SotE main path + optional Remembrances): Dancing Lion, Rellana, Messmer, Romina, Promised Consort Radahn, Bayle→Heart of Bayle, Midra, Metyr, Gaius, Putrescent Knight, Scadutree Avatar, with DLC Remembrance reward goods 2002900–2002910 mapped in `BOSS_REWARD_GOOD_BY_FLAG`. **Click-to-pin**: `BossCard.onTogglePin` toggles the boss's row in the shared `bosses` table store (drops/removes its map pin via `bossPinByFlag`); border/pin-icon reflect pinned state and you can pin several at once — no navigation. **Missing icons**: bosses with no Remembrance (`bossReward()` undefined, e.g. Fire Giant) now render a centered `SwordIcon` fallback on a dark tile instead of a blank card.)

[DONE 2026-06-08] In the bosses data table, we have a column called map, which has entries like m10_00_00_00. That means nothing to the user. We should probably show the map name instead. Maybe we should have a column for the number of runs they will drop or the item or something like that (not firm on those) (`bosses-data-table.tsx` now derives a readable `mapName` per row via `bossMapName(b.mapId)` — which resolves the arena map id to a place name from REGIONS (single-region block → that region's name; multi-region legacy area → the shared area name) with overworld fallbacks ("The Lands Between" / "Land of Shadow") — shown as the "Map" column (string-filterable). A reward column surfaces `bossReward()` (the Remembrance/Heart-of-Bayle drop name + icon) so you can filter by reward. Runes-dropped column was left off — the not-firm idea — in favor of the reward item, which is the more meaningful drop.)

[DONE 2026-06-06] We have an opensource pill thing in the footer and also a view on github button, we do not need both. (removed the "Open source" pill and the View-on-GitHub button)

[DONE 2026-06-06] The website does not have a favicon, we should add one. (favicon.svg given explicit colors + a dark tile and wired into the document head — it existed but was never linked, and used currentColor so it was invisible)

[DONE 2026-06-06] The acknowledgment page is missing a lot of projects that we took inspiration from and does not correctly list how we are using them. We used erdb for the first iteration of the site but since then we have completely switched off because it did not support dlc data. We now have kinda built our own extractor to take its place. docs\cloned-repos-as-docs\er-save-manager this is not even listed. We should go through all the docs\cloned-repos-as-docs\dlc-data-sources that we ended up using or reading for inspiration and add them to the acknowledgment page. (rewrote `credits-section.tsx` into 4 groups — Game data & catalogue / Save files & reverse engineering / Extraction & game formats / References & community — sourcing accurate per-project roles from `packages/vendored-data/README.md`. ERDB demoted to "powered v1, replaced by our own extractor for DLC support". Added 9 missing projects: ER-Save-Lib, er-save-manager, soulstruct's EMEVD role, UXM Selective Unpacker, SoulsFormatsNEXT, Impaler's Archive, elden-ring-eventparam, Elden Ring CT-TGA, Debug Tool, Practice Tool. Licenses verified via GitHub API + LICENSE files.)

[DONE 2026-06-07 → ../calculator.md §0c] On the calcultor page we have a card for "Plan a Build" that looks like you might interact with it, idk kinda confusing. And the reset button stays clickable looking even when you have not interacted with it. (The fake-CTA "Plan a build" banner is gone — replaced by the real playstyle picker ("What do you want to play?"). Reset moved onto the Target-attributes card, disabled until the sliders differ from the seeded preset.)

[DONE 2026-06-07 → ../calculator.md §0c] The calculator does not render weapon icons in the data table, we should add that. (icon column added to the AR table via `TooltipImg(itemIconUrl(icon))`, module-scoped like the inventory tables.)

[DONE 2026-06-07 → ../calculator.md §0] I think the whole calculator page needs to be reworked, the user should be pick an option for like "I want to play a strength build, dex build, arcane build.. etc" and we should be able to tell them what is the best item in the inventory that matches their play style, which one does the most damage based on their stats, where they should place exp point the next time they level up, what weapons would be good to move to once hitting a certain level, etc... I think we need to explore the wiki a lot more and read about different build and possible like weapon tier lists to really make this good. Maybe we want calculators for different things. I could also see like a calculator that recommends when to respec your character if you are very badly specced, or want to change your build. (SHIPPED: playstyle-first "Build Doctor" — 9-archetype picker (auto-detected from save) seeds the shared stat model + filters the AR table; Advisors A–D = best-owned-for-build, best-to-equip-now [build-filtered, with a labelled "strongest overall (any build)" aside], next-level-up [Vigor respected as preference], respec verdict [counts only stranded _offensive_ points; class is permanent]. Build-archetype taxonomy + tier-list/respec research in the doc. Remaining gap: HP/FP/stamina derived-stat curves are hand-fit approximations — labelled "projected (approx)"; extracting the real growth tables is the follow-up. Plus extractor TODO: DLC `wepType`→category mapping.)

[DONE 2026-06-06] On the bosses page when you hover a boss and the tooltip appears, the tool tip content background is white even on the dark theme. This does not look good. (global Tooltip switched from the inverted bg-foreground style to bg-popover with a border — fixes every tooltip)

[DONE 2026-06-06] In the footer we have a button for github stars, that button should have the number of stars as the text. (live stargazers_count fetched from the GitHub API; omitted gracefully if the request fails)

[DONE 2026-06-08] On the map ui, somethings like the runes dropped have a tooltip on hover, but it also has a diffferent popup on click. Some map pins have a popup without a hover tooltip. I think we should standardize this. (standardized on ONE model for every pin kind: **hover tooltip = name + one-line status**, **click popup = full detail card**. Before, only the bloodstain pin had a hover tooltip; graces/bosses/items/player were popup-only, and boss popups were near-empty. Added a `PinKind` discriminant + optional enrichment fields to `MapPin` and two shared kind-aware renderers — `PinTooltipBody` / `PinPopupBody` — in `leaflet-map.tsx`, used by EVERY marker (incl. player + bloodstain, which now route through the same bodies). Enrichment wired in `map-section.tsx`'s pin builders: bosses now show badges + map-area (`bossMapName`) + reward icon/name (`bossReward`) + Defeated/Remaining (via a flag→defeated map shared with the events table, and a new `bossMapIdByFlag` export in `vm/map-pins.ts` so events-sourced boss pins enrich too); items show source label + drop% (when <100) + Owned count + "N locations on map" (when >1) + Collected/Not-collected; graces show region + Discovered/Undiscovered. The old `dangerouslySetInnerHTML` description path was dropped in favour of structured text. Coords kept popup-only, faded (`opacity-60`). Verified in-browser via chrome-devtools: Godrick pin → hover "Godrick the Grafted · Remaining", popup "Demigod · Shardbearer / Stormveil Castle / Remembrance of the Grafted (icon) / Remaining / x,y"; Reduvia item pin → hover "Reduvia · Not collected", popup "Drop / Owned: 0 / Not collected / x,y". Popups use the dark popover surface from the earlier white-bg fix. Typecheck + oxlint + build green.) [white-bg part DONE 2026-06-08] Also even on a dark theme the popup background is white, which does not look good. (fixed in `index.css`: Leaflet's popup/tooltip chrome is hard-coded white in `leaflet.css` and ignores our theme tokens — only the inner React content was themed. Added overrides repainting `.leaflet-popup-content-wrapper` (+ a `--border` outline), `.leaflet-popup-tip`, `.leaflet-popup-close-button`, and `.leaflet-tooltip` (+ its directional `::before` arrows) with the `--popover`/`--popover-foreground` surface, matching the global shadcn Tooltip. The tip square only gets a background, not a border, so the rotated pointer stays clean. `!important` is required: `leaflet.css` is imported inside the map component and loads AFTER `index.css`, so with equal single-class specificity Leaflet's `background: white` won on source order — `!important` forces ours regardless of load order. Verified via chrome-devtools: wrapper computes to rgb(12,10,9) / light text / `--border` outline on the dark theme.)

[DONE 2026-06-09] Some items in our data table place multiple map pins. I think the data table row should how how many pins it will place if selected. Probably want sorting and filtering for this too. (every inventory row now carries `locationCount` (= `itemPins(type,id).length`, joined in `inventory-catalog.ts` alongside `hasCoords`), surfaced as a new **"Locations"** column that REPLACES the old boolean "Has Coordinates" column — a count is strictly more informative and `0` already means "can't be pinned", matching the muted pin cell. Cell renders a filled `MapPinGlyph` + the number (amber) or a muted dash for 0. It's a normal `commonAccessorColumnDef`, so it's sortable AND gets the standard faceted filter chip — and since the count is numeric it now sorts ascending in that facet (see the numeric-facet item below).)

[DONE 2026-06-08] I do not like the solid filled in map pin icon when a map pin is selected. It looses its internal lines in the icon and looks a bit weird. Needs to be fixed repo wide. (root cause: lucide's `MapPinIcon` with `fill="currentColor"` fills BOTH the teardrop body and the inner circle the same colour, so the dot vanishes into the body → featureless blob. Added a shared `components/icons/map-pin-glyph.tsx` (`<MapPinGlyph filled />`) whose filled state draws body + inner circle as ONE `fill-rule="evenodd"` path, making the dot a true cut-out hole that shows the background through — theme-agnostic, and the path matches the actual Leaflet markers (`pinIcon` in `leaflet-map.tsx`). Replaced every filled `MapPinIcon` repo-wide: the inventory/data-table pin toggle (`data-table/common-column-defs.tsx`), the map legend's 6 colour swatches (`sections/map-section.tsx`), and the boss-gallery card marker + "Pinned on map" legend (`sections/story-boss-section.tsx`). Pure-outline `MapPinIcon` usages (quick-select buttons, layer-config icon, boss-badge) were left on lucide since they never fill. Verified in-browser via chrome-devtools — filled glyph renders an amber body with a visible centre hole instead of a blob; typecheck + oxlint + build all green.)

[DONE 2026-06-09] We have the "All, Owned, Missing" segmented controls but I think this sort of conflicts/duplicates the "Quantity" column in some data tables. (resolved by making the toggle and the Quantity facet ONE shared filter — single source of truth — instead of two mechanisms that can diverge. Per the user's steer: keep the prominent toggle (ownership is the #1 filter, "important and unlike others") AND keep the value facet (so you can still filter exact quantities, e.g. "weapons I have 2 of"), just LINK them. The Quantity column's `quantityFilterFn` accepts both shapes: the toggle writes the string presets `'owned'`(qty>0)/`'missing'`(qty===0), the faceted chip writes exact-value arrays like `[2]`, cleared = all. It stays tagged `defaultFacetedFilterFnSymbol` so the chip still shows, and `DataTableFacetedFilter` interprets the `'owned'`/`'missing'` presets into checked boxes so the dropdown visibly reflects the toggle (unchecking one box converts the preset into an explicit value list). The card's old local `useState` pre-filter (which couldn't compose with column filters and wasn't persisted) is gone; the toggle reads/writes the column's filter via a new `useColumnFilterValue(tableId, columnId)` store hook (hydration-gated, shares the persisted `columnFilters` slice the table reads). Toggle highlight derives from that value: preset→that segment, `[2]`→nothing lit (custom selection), cleared→All. So ownership now persists + shows in the toolbar Reset. `data` passed to the table is the unfiltered `items` (react-table applies the filter); the affinity-variant collapse stays a pre-filter. NOTE: the boolean "Has Coordinates" column was also replaced by the numeric "Locations" pin-count column in the same pass — see the multiple-map-pins item above.)

[DONE 2026-06-09] The data table filter facets have the aggregate counts in the popup. I like this but it looks weird for filter columns that are numbers. Like its hard to tell which is the aggregate count and which is the individual item count. Also I think normally the facet filter options should be sorted by aggregate count, except for number columns. The best example of this is the upgrade level column in the weapons data table. (kept both numbers — value + aggregate count — but disambiguated + reordered. **Disambiguation:** the count is now a muted, boxed, monospace pill pushed to the far right (`{n}×`, tooltip "N items"), so it reads as secondary meta vs the plain value label — no more two-bare-numbers ambiguity (`data-table-faceted-filter.tsx`). **Ordering:** `data-table-toolbar.tsx` now sorts the facet options before rendering — numeric columns (Upgrade Level, Locations, Str/Dex/…, FP Cost, …) by VALUE ascending, all other columns by aggregate COUNT descending (most-common first); previously they came out in `getFacetedUniqueValues` row-encounter order, which felt random. Detection: a column is "numeric" when every faceted unique value is a `number`. **Alignment:** the count pill is `ml-auto shrink-0` and the option label is `min-w-0 flex-1 truncate`, so counts are flush-right in a clean column for EVERY facet — text and numeric alike — regardless of label length.)

[DONE (tables) — verified 2026-06-11] In the version of the website currently on prod all table filters, sorts, and even map pans and zooms were stored in a persisted zustland store. This meant if the user refreshed the page was the same. I know we replaced zustland for effect-atom but I think effect-atom can still do persisted state in localstorage just like zustland. we should definetly set that up. (the table half is done: all data-table state — columnFilters, sorting, columnVisibility, columnSizing, columnOrder, rowSelection — persists across refresh via `Atom.kvs`/localStorage in `components/data-table/data-table-store.ts`. The MAP half — pan/zoom/realm — is still transient; tracked as an open item in ../cleanup.md.)

[DONE 2026-06-11] I think all the map chrome still needs another rework. I think we might want to try putting the layers and legend and clear pins and all that stuff as loating on top of the map? Maybe? Not sure Its worth a try. The current ui of a bunch of elements sandwhiched underneath the map does not look great. See main branch of the repo for how it used to look before all the major upgrades. (All map chrome now floats ON the map (`sections/map-section.tsx`): **map switcher** top-left next to Leaflet's zoom control (compact `SHORT_MAP_NAME` labels — "Overworld/Underground/Land of Shadow" — because the manifest names are too wide for an on-map control; wraps on phones), **controls panel** top-right behind a layers-icon toggle (Layers switches + Quick-select presets + "Clear pins (N)", disabled at 0), **"Center on me"** is now a locate icon-FAB bottom-right above the zoom/center readout (title moved to its wrapper div so the disabled explanation still shows — disabled buttons drop pointer events), and the **legend** is a collapsible bottom-left panel with a "Legend" pill when closed. Controls + legend default OPEN on desktop, COLLAPSED on mobile (mobile-first per future/mobile-friendly.md) — `defaultOpen` is read once at overlay mount, which is safe because overlays only mount after the manifest fetch resolves, by which point `useIsMobile` has settled. KEY MECHANISM: the overlays are absolutely-positioned SIBLINGS of the Leaflet container inside the `relative isolate` wrapper — not children — so their pointer/wheel events never reach Leaflet's drag/zoom handlers (no `L.DomEvent.disableClickPropagation` needed); they sit at `z-[1000]`, Leaflet's own control tier, above tile panes (400) and pin popups (700). Panel surfaces are translucent `bg-popover/90 backdrop-blur-sm` (`OVERLAY_PANEL`/`OVERLAY_BUTTON` consts). The freed under-map space went to the map itself: embedded height `min(72vh,720px)` → `min(78vh,860px)`. Verified in real Chromium via Playwright at 1280×800 and 375×667: drag/wheel starting on a panel does NOT pan/zoom the map, drag/wheel on bare map does (pan at min zoom is correctly clamped by maxBounds — pan verified after zooming in), layer switches toggle, realm switching swaps tiles, collapse/expand works for both panels, mobile starts collapsed with everything reachable. Typecheck + oxlint + oxfmt + build green. NOTE: on phones the OPEN controls panel partially covers the map switcher — tolerated since mobile defaults collapsed; revisit in the mobile-friendly audit.)

## Superseded / resolved another way

[SUPERSEDED 2026-06-09, originally DEFERRED 2026-06-06] I do not see a filter or column in the armaments table for if the item is owned or not? Thought that was once there. (at the time: the inventory Armaments table's "Quantity" column IS the ownership indicator — `quantity > 0` = owned, and weapons append ` +N` upgrade level; `main` only had Quantity too, no separate Owned column. An explicit boolean "Owned" column was deferred per request. Since then the All/Owned/Missing segmented toggle landed on every inventory table (see the segmented-controls item above), wired to the Quantity column filter — so ownership filtering exists without the extra column. NOTE: the standalone "Weapons" browser is save-independent and intentionally has no ownership.)

## Planned → tracked in other project docs

[PLANNED 2026-06-07 → ../overview-completion-tracker.md] The overview route still has an empty avatar circle, I know prod had this too it was temporary. we should do something better. (planned: repurpose the circle as a completion ring with the equipped-helm icon in the center — kills the placeholder by making it the page's headline metric)

[PLANNED 2026-06-07 → ../overview-completion-tracker.md] I think the overview page should probably have a breakdown of like what percent of weapons you have collected, what percent of armor, whatever. People will probably want to use that as like a way to see overall game completion at a high level and Items Collected 463 / 5709 across 19 inventory types does nto tell you a whole lot. (planned: overhaul Overview into a shareable 100%-completion tracker with a curated denominator — the 5709 is distorted by ~3265 weapon-variant rows + non-collectible noise; real base-weapon count is 481)

[PLANNED 2026-06-07 → ../overview-completion-tracker.md] I think maybe the overview screen should have data tables just for like collected inventory of all different item types. This might need to be a whole seperate project doc that we plan out how to show this in the ui but like sometimes a user might want to look at a data table of everything in game collected and not collected (with a filter for owned and not owned). and sometimes they might want to look at a data table of just the items they have collected. (planned: split across the two surfaces — Overview = curated completion tracker; Inventory tables get an All/Owned/Missing segmented filter, pairing with the existing affinity-variant toggle. The inventory-table half has since shipped — see the segmented-controls item above.)

[PLANNED 2026-06-09 → sharing-feature.md — SINCE SHIPPED, doc now at ./sharing-feature.md] I would like to have like a sharing feature so that one user could share their stuff with a different user. The best way to do this is if we could encode the entire save file in the query params. We need to know how big the extracted part of the save file is and how long query params could be. We could potentially compress it first to make this work. (mapped out with measured numbers: raw save is ~29 MB so the whole binary is out, but the _extracted_ unit is one `LeanSlot` — render-only ≈ 7 KB / full-fidelity ≈ 11 KB in the query string via brotli+base64url. Binding constraint is the SSR/proxy request-line limit, not the browser: <8 KB clears stock nginx, ≤16 KB clears Node/Vercel/Cloudflare. Recommendation: share ONE slot, render-only, `?save=` global search param validated with effect `Schema.toStandardSchemaV1` + `retainSearchParams(['save'])` so it survives navigation.)
